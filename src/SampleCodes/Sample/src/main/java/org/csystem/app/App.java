/*----------------------------------------------------------------------------------------------------------------------
    Sınıf Çalışması: Komut satırı argümanı olarak aldığı bir önek (prefix), n, count, ve periyot bilgisi için n tane dosyaya
    ayrı ayrı rasgele üretilmiş count tane şifreyi verilen periyotta yazan programı yazınız. Dosyaların isimleri <önek>-<numara>
    biçiminde olacaktır. Programı Scheduler sınıfı ile yazınız.
    005-PeriodicAsyncPasswordGeneratorSimulation
    006-PeriodicAsyncPasswordGeneratorSimulation
----------------------------------------------------------------------------------------------------------------------*/
package org.csystem.app;

import java.util.Timer;

class App {
    public static void main(String[] args)
    {
        var timer = new Timer();
        /*
        var cs1 = new CounterScheduler(5, 1000);
        var cs2 = new CounterScheduler(5, 1000);

        cs1.schedule(() -> Console.write("."), () -> Console.writeLine("\nFinished"));
        cs2.schedule(() -> Console.write("$"), () -> Console.writeLine("\nFinished"));
        //cs1.schedule(() -> Console.write("."), () -> Console.writeLine("\nFinished"));

         */
    }
}

